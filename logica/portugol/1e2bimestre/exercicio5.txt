programa {
  funcao inicio() {
     real m
    real n
   
   escreva ("Digite o primeiro numero: ")
    leia (n)
    escreva ("Digite o segundo numero: ")
    leia (m)
       se  (m > n) {escreva(m ,"É maior que", n)}
   se (m < n) {escreva(n , "É maior que" , m)}
   senao {escreva(m," e " , n , " são numeros iguais")}
 
  


    
  }
}
